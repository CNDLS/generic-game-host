describe("YAML : !do", function () {

    // create a YAML object that contains an !evaluate node.
	//     var test_yaml = new YAML();
	//
	//     describe("!evaluate ", function () {
	//         it("!evaluate requires a first parameter -- call: <function>", function () {
	//             expect(??).toEqual(??);
	//         });
	// }
});